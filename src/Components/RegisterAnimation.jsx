import Lottie from "lottie-react";
import register from "../assets/animation/signup.json";
const RegisterAnimation = () => {
  return <Lottie animationData={register} loop={true} />;
};

export default RegisterAnimation;
